小米 5 的 android.os.Build 信息打印结果如下：

```java
Build.MODEL == MI 5 // 用户可见的最终产品名称
Build.PRODUCT == gemini// 整个产品的名称
Build.HARDWARE == qcom
Build.FINGERPRINT == Xiaomi/gemini/gemini:7.0/NRD90M/8.1.25:user/release-keys// 该编译的唯一标识
Build.BOOTLOADER == unknown
Build.BRAND == Xiaomi// 产品/硬件所关联的用户可见的品牌
Build.MANUFACTURER == Xiaomi // 厂商
Build.BOARD == QC_Reference_Phone// 底层板名称
Build.DISPLAY == NRD90M// 显示给用户的编译 ID
Build.ID == NRD90M
Build.TAGS == release-keys
Build.TYPE == user
Build.SUPPORTED_ABIS == [Ljava.lang.String;@643d3ed
Build.SUPPORTED_32_BIT_ABIS == [Ljava.lang.String;@bc1a222
Build.SUPPORTED_64_BIT_ABIS == [Ljava.lang.String;@92494b3
Build.HOST == c3-miui-ota-bd88.bj
Build.TIME == 1516821747000
Build.USER == builder
```

